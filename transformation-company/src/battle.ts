import { Transformer } from './shared/types/transform';

export function fight(transforms: Array<Transformer>): Array<string> {
  return [
    '1 battle',
    'Winning team (Decepticons): Soundwave',
    'Survivors from the losing team (Autobots): Hubcap ',
  ];
}
